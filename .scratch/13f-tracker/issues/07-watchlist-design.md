# 07 — Watchlist design: naming, contents, update semantics

Type: grilling
Status: open
Blocked by: 01 (resolved), 06 (open)

## Question

Given the confirmed filer list (01) and what TradingView writes are actually possible
(06): settle the per-manager watchlist design — naming prefix, whether lists hold all
holdings or top-N, how non-mappable symbols (delisted, foreign, options rows) are
handled, and update semantics on quarterly refresh (full sync with deletions vs
additive). Decided with Philip; graduates the watchlist fog item.
